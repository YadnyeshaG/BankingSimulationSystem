# 🏦 Bank Simulation System

**Bank Simulation System** is a robust .NET application that simulates basic banking operations, built with **ASP.NET Core MVC** and **Web API**. It provides a user-friendly interface for managing accounts, performing secure transactions, and viewing transaction history.

---

## ✨ Features

* **Secure Authentication:** User registration and login utilizing **JWT-based authentication**.
* **Account Management:** Functionality to effortlessly **create and view** bank accounts.
* **Transactions:** Core banking operations, including **depositing and withdrawing** money.
* **Transaction History:** Comprehensive tracking of all deposits and withdrawals.
* **Dashboard:** A clear view of account details, including current balance, account number, and user name.
* **MVC Frontend:** Clean and interactive UI built using **ASP.NET Core MVC**.
* **Database Integration:** Reliable data persistence using **Entity Framework Core** with **SQL Server**.

---

## 💻 Technologies Used

* ASP.NET Core MVC & Web API
* Entity Framework Core
* SQL Server
* JWT Authentication
* C#

---

## ⚙️ Requirements

To run this project locally, you will need:

* **Visual Studio 2022** (Ensure the *ASP.NET and web development* workload is installed).
* **.NET 7 SDK** (or the version specified in the project file).
* **SQL Server** (local or remote instance).
* SQL Server Management Studio (SSMS) (Optional).

